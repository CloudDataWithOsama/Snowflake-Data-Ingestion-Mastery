-- 1. Create target table
CREATE OR REPLACE TABLE CUSTOMER_DETAILS_UI (
    ID INT,
    NAME STRING,
    EMAIL STRING,
    CITY STRING
);

-- Note: Use the 'Load Data' button in Snowsight to upload your CSV.